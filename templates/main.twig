{% extends "base.twig" %}

{% block body %}

    <div class="container pt-5">
        <div class="row">
            <div class="col-8">
                <p class="text-center">Добро пожаловать! Для дальнейших действий зарегистрируйтесь на сайте или войдите
                    под своими учётными данными на сайт.</p>
            </div>
            <div class="col-4">
                <div class="row">
                    <div class="col-4">
                        <a href="{{ url_for('registration') }}" class="btn btn-primary text-wrap">Регистрация</a>
                    </div>
                    <div class="col-4">
                        <a href="{{ url_for('authorization') }}" class="btn btn-primary">Войти</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock body %}